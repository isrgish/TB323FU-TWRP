# Charger
ro.charger.enable_suspend=true

# FUSE passthrough
persist.sys.fuse.passthrough.enable=true

# For keymint to properly start.
vendor.gatekeeper.is_security_level_spu=1
ro.crypto.dm_default_key.options_format.version=2
keymaster_ver=4.x
